{
  "name": "verify-push-serverless",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy",
    "ci:deploy-functions": "twilio-run deploy --load-system-env --env .env.example --account-sid=$ACCOUNT_SID --auth-token=$AUTH_TOKEN --production --override-existing-project"
  },
  "dependencies": {
    "googleapis": "^67.0.0",
    "twilio": "^3.55.1"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "10"
  }
}
